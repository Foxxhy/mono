import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Components/Toast" />

# Toast

## Sommaire

- [Quand utiliser](#quand-utiliser)
- [HTML minimal copiable](#html-minimal)
- [Variantes](#variantes)
- [RGAA et accessibilité](#rgaa)
- [À éviter](#à-éviter)

<h2 id="quand-utiliser">Quand utiliser</h2>

Composant de la famille **Feedback**. Utilisez les classes du bundle `dist/design-system.css` et gardez le HTML natif autant que possible.

<h2 id="html-minimal">HTML minimal copiable</h2>

```html
<div class="toast toast-success" role="status" aria-live="polite">
  <span class="toast-icon" aria-hidden="true">✓</span>
  <div class="toast-content"><p class="toast-title">Enregistré</p><p class="toast-message">Vos changements sont sauvegardés.</p></div>
  <button class="toast-close" aria-label="Fermer la notification">×</button>
</div>
```

<h2 id="variantes">Variantes</h2>

Les variations visuelles sont disponibles dans l’onglet stories de cette page (`Components/Toast`). Les classes proviennent de `packages/design-system/src/components/toast.scss`.

<h2 id="rgaa">RGAA et accessibilité</h2>

- Les toasts dynamiques doivent être annoncés via `role="status"`/`aria-live="polite"`; les erreurs urgentes peuvent utiliser `role="alert"`.
- Conserver un ordre de focus logique et visible.
- Ne pas transmettre une information uniquement par la couleur.
- Préférer les éléments HTML natifs avant d’ajouter des rôles ARIA.

<h2 id="à-éviter">À éviter</h2>

- Remplacer un élément natif interactif par une `div` cliquable.
- Supprimer le focus visible ou le libellé accessible.
- Copier les couleurs hexadécimales : utiliser les classes et tokens du design system.
